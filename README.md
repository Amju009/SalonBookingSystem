# Salon Booking System API

This is a backend application built using Node.js, Express, and TypeScript.

Features:
- User registration and login (JWT)
- Service management (Admin)
- Booking system (User + Guest)
- Admin approval/decline
- Clean architecture design
- Test coverage > 80%
